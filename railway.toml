[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build -w @hospitalityos/shared && npm run build:self -w sovereign-drive-engine"

[deploy]
startCommand = "npm run start -w sovereign-drive-engine"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
