{
  "name": "data-infrastructure",
  "private": true,
  "workspaces": [
    "packages/*",
    "sovereign-drive-engine",
    "services/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -w @hospitalityos/shared && npm run build -w @datazone/db && npm run build -w @datazone/sdk && npm run build -w sovereign-drive-engine && npm run build -w @datazone/service && npm run build -w @hospitalityos/api && npm run build -w @datazone/web",
    "test": "npm run build -w @hospitalityos/shared && npm run build -w sovereign-drive-engine && npm run test -w sovereign-drive-engine && npm run test -w @hospitalityos/api",
    "test:engine": "npm run test -w sovereign-drive-engine",
    "dev:engine": "npm run dev -w sovereign-drive-engine",
    "dev:datazone": "npm run dev -w @datazone/service",
    "dev:web": "npm run dev -w @datazone/web",
    "db:generate": "npm run db:generate -w @datazone/db",
    "db:push": "npm run db:push -w @datazone/db"
  },
  "engines": {
    "node": ">=20"
  }
}
