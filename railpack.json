{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": ["npm ci"]
    },
    "build": {
      "inputs": [{ "step": "install" }],
      "commands": [
        "npm run build -w @hospitalityos/shared",
        "npm run build -w @datazone/db",
        "npm run build:self -w @datazone/service"
      ]
    }
  },
  "deploy": {
    "startCommand": "npm run start -w @datazone/service"
  }
}
